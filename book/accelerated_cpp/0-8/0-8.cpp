/* 0-8 
 *
 * Yes. The // comments *whatever* comes after.
 */

#include <iostream>

int main() {
    // This is a comment that extends over several lines
    // by using // at the beggining of each line instead of using /*
    // or */ to delimit comments.
    std::cout << "Does this work?" << std::endl;
    return 0;
}
