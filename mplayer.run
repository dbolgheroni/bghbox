#!/bin/sh

FILE=$@

#mplayer -vo xv -cache 65536 -cache-min 50 \
#    -font 'Liberation Sans:style=Bold' \
#    -utf8 -hardframedrop \
#    -ass -ass-color FFFF0000 -ass-font-scale 2 $FILE

if [ -z $FILE ]; then
    echo "usage: $0 file"
else
mplayer -vo gl -cache 131072 -cache-min 50 \
    -font 'Liberation Sans:style=Bold' \
    -utf8 -hardframedrop -nocorrect-pts $FILE
fi
